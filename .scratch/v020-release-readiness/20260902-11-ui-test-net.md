---
title: 11 UI 层回归测试网
status: backlog
type: task
labels: [needs-triage]
---

## 背景（Standards judgement call）

`poweri/components|features|layout` 无任何测试文件：`ChatInput.tsx` 2583 行约 25 个 `useState`（附件/斜杠/@提及/历史/文件索引/休眠六个关注点）、`AppShell.tsx` 2493 行 24 个、`SkillsMarketView.tsx` 1508 行 14 个。替换件允许大，但本周期全是改动热点，无回归网。部分纯函数（如 `buildSlashCommandLayout`）已刻意导出、具备可测性却未测。

## 为什么是 needs-triage

测试策略需要拍板：只测已导出纯函数（低成本、零依赖）vs 引入组件测试依赖（testing-library 等，需评估与 node --test 体系与构建的兼容）vs 先拆关注点再测。发布前不做，0.2.x 规划。
